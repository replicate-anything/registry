# Draft registry stubs

Stubs here are **not** indexed in `index.csv` or the Shiny study dropdown.

Move a stub back to `papers/<folder>.yml` and re-render `index.qmd` when the study is ready for publication.
